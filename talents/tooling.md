# Tooling

Building and maintaining developer tooling.

- Build systems, test runners, linters, formatters, pre-commit hooks
- Reproducibility: the same command produces the same result on any machine
- Speed budgets: every second of build time multiplied by every developer
- Diagnostics that point at the cause, not the symptom
- Tooling that fails closed and leaves the working tree clean
