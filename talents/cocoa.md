# Cocoa

AppKit / Cocoa framework fluency.

- Document architecture: NSDocument, NSWindowController, model–view–controller
- Bindings, KVO, KVC — when each pays its keep and when each becomes a debugging hazard
- Drawing: NSView, layer-backed views, Core Animation interop
- Menu bar, Dock integration, accessibility support
- Sandbox, entitlements, app-translocation, codesign realities
