# Browsers

Browser engine internals and runtime characteristics.

- Rendering pipeline: parse → DOM → style → layout → paint → composite
- JS engine internals: hot paths, JIT tiers, memory layout
- Network stack: HTTP/2, HTTP/3, DNS, TLS handshake costs
- DevTools fluency: profiler, memory snapshots, network waterfall
- Cross-browser compatibility realities and feature-detection patterns
