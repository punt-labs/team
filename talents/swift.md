# Swift

Modern Swift implementation discipline.

- Types, protocols, generics, opaque return types, existentials
- Structured concurrency, actors, Sendable, @MainActor isolation
- SwiftUI + Observation framework; `@Bindable`, `@FocusState`
- Protocol-oriented design over class hierarchies
- SwiftFormat + SwiftLint must pass — fix the format config, not the lint rule
