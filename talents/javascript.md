# JavaScript

Browser-grade JavaScript and runtime semantics.

- ECMAScript spec literacy: hoisting, closures, the event loop, microtasks
- Web Platform APIs (fetch, URL, Crypto.subtle, structured clone) over polyfills
- ESM modules; CommonJS interop only when forced
- Performance budgets: bundle size, time-to-interactive, render budget
- Browser compatibility realities: caniuse, MDN, progressive enhancement
